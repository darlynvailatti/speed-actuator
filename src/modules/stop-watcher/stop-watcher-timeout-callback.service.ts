import { Injectable, Logger } from "@nestjs/common";

@Injectable()
export class StopWatcherTimeoutCallbackService {

    private readonly logger = new Logger(StopWatcherTimeoutCallbackService.name)

    constructor(
        
    ) { }

}